//Buttons listed on the email page
//Made of email icon and select text

interface TableProps {
    buttonText: string;
  }

const EmailButton: React.FC<TableProps> = ({buttonText})  => {
  let icon = "/test-qr.png"; 
  return (
  <>
    <button>

    </button>
  </>
  );
};

export default EmailButton;